header(class="h-16 w-full bg-pink-400 shadow sticky top-0 z-30 flex items-center justify-between px-4 md:px-6")

  div(class="flex items-center space-x-3")
    button(
      id="btn-hamburger"
      class="p-2 text-white hover:bg-pink-500 rounded-lg transition-colors"
    )
      i(class="fi fi-rr-menu-burger text-xl")

    p(class="font-semibold text-lg text-gray-100")=pagina

  p(class="text-xm text-gray-100 font-semibold")
      i(class="fi fi-rr-user text-lg mr-1.5")
      span Hola #{usuario.nombreUsuario}

      
