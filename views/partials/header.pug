header(class="headerNav")

  div(class="flex items-center space-x-3")
    button(
      id="btn-hamburger"
      class="p-2 text-gh-primaryHover hover:bg-pink-500 rounded-lg transition-colors"
    )
      i(class="fi fi-rr-menu-burger text-xl")

    p(class="font-semibold text-lg text-gh-primaryHover/85")=pagina

  p(class="text-xm text-purple-700 font-semibold")
      i(class="fi fi-rr-user text-lg mr-1.5")
      span Hola #{usuario.nombreUsuario}

      
