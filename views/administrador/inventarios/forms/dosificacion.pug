form(action="", method="post" id="formularioProducto" enctype="multipart/form-data" class="col-span-12 grid grid-cols-12 gap-6 items-start")


    //- COLUMNA IZQUIERDA
    input(type="hidden", name="_csrf" value = csrfToken)
    div(class="col-span-12 md:col-span-8 grid grid-cols-12 gap-4 cards cardsWhite p-6")
        
        //- Nombre del Producto
        //- SEPARADOR 100% (ATRIBUTOS)
        div(class="col-span-12 -mx-6 mt-4")
            
            h1.h1(class="text-left pl-11 flex items-center text-gray-700")
                i.fi-rr-features.pr-2
                | Datos Básicos

        div(class="col-span-8")
            .div(class="alert alert-warning")
                div(class="alert-icon")
                    i.fi-rr-light-emergency-on
                | Los productos que serán dosificados irán a la bodega de tránsito.

        div(class="col-span-4")
            label.label Cuántas unidades por paquete?
            input(type="number" id="unidadesPorPaquete" name="unidadesPorPaquete"  min="1" max="30"  value="12" class="field-text text-5xl text-5xl bg-gray-100 cursor-not-allowed opacity-75" readonly)



       
        //ROWS
        input(type="hidden", name="idProducto[]")
        div(class="col-span-12 md:col-span-3")
            label.label Sku 
            input(type="text" id="sku[]" name="sku[]" class="field-text" placeholder="ABD-CDS" )
        
        div(class="col-span-12 md:col-span-3")
            label.label Nombre del Producto
            input(type="text"  id="name[]" name="name[]" class="field-text" placeholder="Camisa Verde" disabled )
            #nombreProducto
        
        div(class="col-span-12 md:col-span-2")
            label.label Cantidad 
            input(type="number" id="cantidad[]" name="cantidad[]"  min="1" max="1000000" placeholder="1000" class="field-text")
        
        div(class="col-span-12 md:col-span-2")
            label.label Valor Unidad 
            input(type="text" id="valorUnidad[]" name="valorUnidad[]"  placeholder="$30.000" class="field-text")
        
        div(class="col-span-12 md:col-span-1 pt-3")
            a(class="btn btn-secondary" id="addMore")
                i.fi-rr-square-plus
                |Agregar Mas       





       






                            
    //- COLUMNA DERECHA
    div(class="col-span-12 cards cardsWhite md:col-span-4 flex flex-col gap-4")
        div(class="p-6")
            h1.h1
                i.fi-rr-dashboard-monitor.pr-2
                |Monitor de Dosificación
            //- Contenedor Flex para ponerlos uno al lado del otro
            

    div(class="col-span-12")
        div(class="flex  justify-center")
            button(class="btn btn-primary w-full"  type="submit" id="guardar" )
                i.fi-rr-disk
                | #{btnName}



script(src="/js/dataDose.js" type="module") 
script(src="https://cdn.jsdelivr.net/npm/sweetalert2@11")           