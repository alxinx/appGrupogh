div(class="tarjetaBase")
    div(class="flex items-start justify-between gap-4")
        div(class="flex-1 min-w-0")
            p(class="text-sm font-medium text-slate-500 mb-3")
                | Clientes VIP
            h3#stat-cli-vip(class="text-3xl font-bold text-slate-800") —
            p(class="text-xs text-slate-400 mt-1") Alto valor o alta frecuencia
        div(class="w-12 h-12 rounded-xl bg-amber-50 flex items-center justify-center flex-shrink-0")
            i(class="fi fi-rr-star text-amber-500 text-xl")

    div(class="mt-4 flex items-start gap-2 text-[10px] text-slate-500 bg-slate-50 px-3 py-2 rounded-lg leading-relaxed")
        i(class="fi fi-rr-info flex-shrink-0 mt-0.5")
        span 5+ compras en 2 sem. · o 3+ compras ≥ $1.000.000 en el mes
