div(class="tarjetaBase")
    div(class="flex items-start justify-between gap-4")
        div(class="flex-1 min-w-0")
            p(class="text-sm font-medium text-slate-500 mb-3 flex items-center gap-2")
                | Clientes Nuevos
            h3#stat-cli-nuevos(class="text-3xl font-bold text-slate-800") —
            p(class="text-xs text-slate-400 mt-1") Registrados esta semana
        div(class="w-12 h-12 rounded-xl bg-blue-50 flex items-center justify-center flex-shrink-0")
            i(class="fi fi-rr-user-add text-blue-500 text-xl")

    div#stat-cli-nuevos-trend(class="mt-4 hidden items-center gap-2 text-xs font-semibold px-3 py-2 rounded-lg")
        i#stat-cli-nuevos-icon
        span#stat-cli-nuevos-pct
