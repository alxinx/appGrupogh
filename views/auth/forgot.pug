extends ../layout/index
block contenedor
    // Card
    div(class="w-full max-w-md bg-white rounded-xl shadow-lg p-6 sm:p-8")
      
      // Logo placeholder
      div(class="flex justify-center mb-6")
            img(src="/img/logo.png" width="100" alt="")

      // Title
      h1(class="h1")
        | Como recuperar tu contraseña?
      
      p(class="p")
        | Solo ingresa tu correo electronico y te enviaré un link para que restablescas la contraseña😎

      // Form
      form(class="space-y-4")
        
        // Email
        div
          label(class="label")
            |Tu Email
          input(
            type="text"
            placeholder="Ingresa el email con que estas registrado"
            class="field-text"
          )

        // Forgot password
        div(class="flex justify-end")
          a(href="/", class="link")
            | Volver a loguearse 

        // Button
        button(type="submit" class="btn-primary")
    
          | Recuperar